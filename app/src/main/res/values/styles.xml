<resources>

    <style name="MyActionBar" parent="Theme.AppCompat">
        <item name="android:background">@color/toolbarColor</item>
        <item name="android:titleTextStyle">@style/MyTextTitle</item>
    </style>

    <style name="MyTextTitle" parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/mainTitleColor</item>
    </style>

</resources>
